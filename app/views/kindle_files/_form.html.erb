<%= form_for(@kindle_file) do |f| %>
  <% if @kindle_file.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@kindle_file.errors.count, "error") %> prohibited this kindle_file from being saved:</h2>

      <ul>
      <% @kindle_file.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
